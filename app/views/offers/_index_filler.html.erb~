  <tr>
    
    <td><%=link_to( image_tag(offer.image.url(:small)), offer_path(offer), :class=>"work") %></td>
    <td><%= link_to offer.title, offer_path(offer) %></td>
    <td><%= offer.location_list %></td>
    <td><%= offer.tag_list %></td>
    <td><%= offer.updated_at %></td>
  </tr>
